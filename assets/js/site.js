(()=>{const root=document.documentElement,stored=localStorage.getItem("theme"),system=matchMedia("(prefers-color-scheme: dark)").matches;const setTheme=t=>{root.dataset.theme=t;localStorage.setItem("theme",t);document.querySelector("meta[name=theme-color]")?.setAttribute("content",t==="dark"?"#171a18":"#f8f8f6")};setTheme(stored||(system?"dark":"light"));document.querySelector(".theme-toggle")?.addEventListener("click",()=>setTheme(root.dataset.theme==="dark"?"light":"dark"));const btn=document.querySelector(".menu-toggle"),menu=document.querySelector(".mobile-menu");btn?.addEventListener("click",()=>{const open=btn.getAttribute("aria-expanded")=="true";btn.setAttribute("aria-expanded",String(!open));menu.hidden=open});const links=[...document.querySelectorAll(".post-row a")];document.querySelectorAll("[data-random-post]").forEach(a=>a.addEventListener("click",e=>{if(!links.length)return;e.preventDefault();location.href=links[Math.floor(Math.random()*links.length)].href}));const body=document.querySelector(".post-body"),label=document.querySelector(".reading-time"),bar=document.querySelector(".reading-progress span");if(body&&label){label.textContent="약 "+Math.max(1,Math.ceil(body.innerText.trim().split(/\s+/).length/220))+"분 읽기"}if(bar){const update=()=>{const max=document.documentElement.scrollHeight-innerHeight;bar.style.width=(max?Math.min(100,scrollY/max*100):0)+"%"};addEventListener("scroll",update,{passive:true});update()}document.querySelectorAll("pre").forEach(pre=>{const b=document.createElement("button");b.className="copy-code";b.type="button";b.textContent="복사";b.addEventListener("click",async()=>{try{await navigator.clipboard.writeText(pre.querySelector("code")?.innerText||pre.innerText);b.textContent="복사됨";setTimeout(()=>b.textContent="복사",1600)}catch{b.textContent="실패"}});pre.append(b)})})();