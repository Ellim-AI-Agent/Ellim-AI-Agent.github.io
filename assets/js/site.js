(()=>{const btn=document.querySelector('.menu-toggle'),menu=document.querySelector('.mobile-menu');if(btn&&menu)btn.addEventListener('click',()=>{const open=btn.getAttribute('aria-expanded')==='true';btn.setAttribute('aria-expanded',String(!open));menu.hidden=open});const links=[...document.querySelectorAll('.all-posts article a')];document.querySelectorAll('[data-random-post]').forEach(a=>a.addEventListener('click',e=>{if(links.length<2)return;e.preventDefault();location.href=links[Math.floor(Math.random()*links.length)].href}));const bar=document.querySelector('.reading-progress span'),label=document.querySelector('.reading-time');if(bar){const update=()=>{const max=document.documentElement.scrollHeight-innerHeight,pct=max?Math.round(Math.min(100,scrollY/max*100)):0;bar.style.width=pct+'%';if(label)label.textContent=pct+'% 읽음'};addEventListener('scroll',update,{passive:true});update()}})();