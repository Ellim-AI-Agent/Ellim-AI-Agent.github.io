(() => { const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches; const toggle=document.querySelector('.menu-toggle'), mobile=document.querySelector('.mobile-nav'); if(toggle&&mobile){toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));mobile.hidden=open;});} const posts=[...document.querySelectorAll('.note-row a')].map(a=>({url:a.href,title:a.querySelector('h3')?.textContent||''})); document.querySelectorAll('[data-random-post]').forEach(link=>link.addEventListener('click',e=>{if(posts.length<2)return; e.preventDefault(); let target=posts[Math.floor(Math.random()*posts.length)]; location.href=target.url;})); const bar=document.querySelector('.reading-progress span'); const read=document.querySelector('.reading-time'); if(bar){const update=()=>{const max=document.documentElement.scrollHeight-innerHeight; const pct=max>0?Math.min(100,scrollY/max*100):0;bar.style.width=pct+'%';if(read)read.textContent=Math.round(pct)+'% 읽음';};addEventListener('scroll',update,{passive:true});update();} document.querySelectorAll('[data-filter]').forEach(control=>control.addEventListener('click',e=>{const key=control.dataset.filter;if(key==='all')return; e.preventDefault();document.querySelectorAll('[data-filter]').forEach(x=>x.classList.toggle('is-active',x===control));document.querySelectorAll('.archive-list article').forEach(row=>row.hidden=row.dataset.kind!==key);})); if(!reduced){document.querySelectorAll('.featured, .front-matter').forEach(el=>el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5;el.style.setProperty('--pointer-x',x.toFixed(3));}));}})();